# X3 / FA61 protocol target

This page is the model-level entry point for devices that enumerate as USB PID `0xfa61` in the driver's `x3-wired` mode. Packet definitions live in [`../protocols/`](../protocols/README.md); transport behavior lives in [`../transports/`](../transports/README.md).

## Identity and aliases

Tested or analyzed hardware may be sold as Attack Shark X3, FA61, or Kysona M600. They are treated as one protocol target for now, but branding alone does not prove identical firmware behavior.

The BLE names `M600-5.2` and `M600-5.4` identify RF/host slots, not firmware versions. Both strings occur in the same firmware payload; `NVDS_TAG_RF_MODE` selects the advertised name. A short pairing-button press toggles slots and a long press enters pairing for the selected slot. See the [BLE transport reference](../transports/ble-gatt.md#device-identity) and [correction ledger](../research/corrections.md).

## Support matrix

| Feature | X3/FA61 wired | X3/M600 BLE | Canonical reference |
|:--------|:--------------|:------------|:--------------------|
| Open/configure | Production USB path | Experimental tooling | [Transports](../transports/README.md) |
| Configuration readback | Live-confirmed with `0xa0` selector | FEE4 notifications/read paths | [USB HID](../transports/usb-hid.md#fa61-configuration-readback) |
| DPI, 1–8 stages | Live-confirmed | Live-confirmed | [`0x04`](../protocols/04-dpi.md) |
| Sensor toggles | Capture/live-confirmed in `0x04` | Same packet accepted | [`0x04`](../protocols/04-dpi.md) |
| Preferences/debounce | Live-confirmed, builder checksum gap | Live-confirmed with safety restriction | [`0x05`](../protocols/05-preferences.md) |
| Polling rate | Capture/live-confirmed | Rejected; do not send | [`0x06`](../protocols/06-polling-rate.md) |
| Button mapping | Normal buttons live-confirmed; builder checksum gap | Checksum acceptance live-confirmed | [`0x08`](../protocols/08-button-mapping.md) |
| Custom macros | Live-confirmed | Parser acceptance only | [`0x09`](../protocols/09-custom-macros.md) |
| Reset/profile load | Live/capture-confirmed | Parser acceptance; persistence incomplete | [`0x0c`](../protocols/0c-profile-reset.md) |
| Battery | Unavailable while wired | Standard Battery Service works | [Battery](../protocols/battery.md) |

## Device-specific caveats

- Button mapping writes a complete model-default packet. Unspecified buttons are reset to driver defaults; this is not a partial update of current device state.
- Direct scroll remaps are unsafe. An assigned action may repeat indefinitely until unplug or reboot.
- DPI-button remaps appear to be ignored by stock FA61 firmware.
- Hardware without an RGB base shows no visible response to RGB fields. Sleep, deep-sleep, and light-field semantics remain incompletely characterized on X3.
- Report `0x05` with light-mode byte `0x00` has crashed X3 firmware over BLE. Follow the restrictions in [`../safety.md`](../safety.md).
- Reset must use the model-correct report sequence and conservative delays. Stock X3 captures use roughly 500 ms between reports.
- Reports `0x04`, `0x05`, and `0x08` are target-profile operations on X3/M600. Their armed read selectors can load another working profile without changing persistent `0x0c` metadata. The 2026-07-17 X3/M600 “metadata-only” result was corrected after this branch was recovered.

## Reusable X3/M600 profile A/B probe

[`scripts/fa61-profile-ab.ts`](../../scripts/fa61-profile-ab.ts) runs the same guarded
FA61 Col04 experiment against either mouse. It defaults to 500 ms after every HID selector
or write, snapshots reports `0x0b`, `0x0c`, `0x04`, `0x05`, and `0x08`, and supplies the
intended one-based profile target on every `0x04`/`0x05`/`0x08` read and write.

Preview all packets without opening hardware:

```bash
bun scripts/fa61-profile-ab.ts --dry-run
```

Run one labeled hardware session and keep the generated JSON outside the repository:

```bash
bun scripts/fa61-profile-ab.ts --write --label X3 --out C:/temp/x3-profile-ab.json
```

Add `--with-cycle` only when direct profile switching succeeds; it temporarily binds
Forward to `34 00 00`, tests one press, and restores the prior button report. Run
`--help` for DPI, delay, device-selection, and restoration options.

Correctly targeted 2026-07-17 runs succeeded on both tested mice: profiles 1 and 2 loaded
distinct DPI images under software selection and after power-cycle, and one physical
Forward press bound to `34 00 00` changed profile 1 to profile 2. The runner now treats
malformed armed-read observations as transient and restores DPI, preferences, and button
reports after an unexpected error.

## Readback latency benchmark

[`scripts/fa61-readback-benchmark.ts`](../../scripts/fa61-readback-benchmark.ts) performs
validated, read-only `0x0b`, `0x0c`, `0x04`, and `0x08` transactions with both a
persistent HID handle and one handle per transaction:

```bash
bun scripts/fa61-readback-benchmark.ts --label X3 --out C:/temp/x3-readback.json
```

A conservative preliminary A/B used five warm-ups and twenty measured transactions per
report and per mode. With a persistent handle, both mice had approximately 2 ms median
selector SET time, 3 ms to complete the first ready-status read, and 3 ms target GET
time. Neither mouse was consistently faster. Opening a handle had a median cost of
roughly 7–8 ms for X3 and 10–11 ms for M600 in these sequential sessions, but that
host-side difference is not evidence of faster mouse firmware.

**Live-confirmed on M600, 2026-07-17:** the 43 measured profile-metadata attempts
included three malformed packets with header `0c 0a 02 ...` and valid complements. The
initial X3 session also returned three version packets with prefix `0b 08 02` among 40
measured version reads. A repeated X3 strict-validation session observed one version
rejection (caused by an opened-but-then-erroring handle, not a publication race) across
160 measured transactions and no further cross-report contamination. The M600 publication
race is reproducible at this sample size; the X3 was clean once warm-up had drained any
leftover mailbox state. The X3 cross-report evidence must not be reported as an
independent fault.

Raw benchmark and session JSON is archived under
[`../evidence/x3-fa61/`](../evidence/x3-fa61/README.md#probing-sessions-2026-07-17).


## Production implementation gaps

The protocol behavior is confirmed, but the current production `UserPreferencesBuilder` and `MacrosBuilder` still emit legacy X11 checksum layouts. X3 requires model-specific 16-bit big-endian checksums for reports `0x05` and `0x08`. The report pages distinguish confirmed device behavior from current implementation status.

## Evidence and open questions

Raw X3/FA61 evidence is catalogued under [`../evidence/x3-fa61/`](../evidence/x3-fa61/README.md). Analysis provenance and dated experiments live under [`../research/`](../research/README.md).

Open questions include timing bounds for serialized edge-triggered profile changes, profiles 3 through 5 and multi-step physical cycling, native access to an already bonded BLE device, sleep/deep-sleep behavior, and full multi-page `0x09` macro behavior over BLE. An ACK or matching report-`0x0c` metadata readback proves metadata acceptance, not completion of deferred profile application or persistence.
